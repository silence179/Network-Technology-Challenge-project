# CloudNet 仪表盘系统 — PPT 大纲

---

## 第 1 页 · 封面

CloudNet 空天地一体化网络仪表盘。实时仿真控制 · 资源池管理 · 策略驱动的 SDN 闭环。

---

## 第 2 页 · 总览

仪表盘共 9 张卡片，分 3 个标签页。

控制闭环（3 张）：动态拓扑仿真控制驱动 54 节点、648 链路、6000 步实时推演；规则翻译将 36,000 条 S3 路由规则转为 OpenFlow 流表并下发；网络拓扑展示 ONOS 实时发现的物理网络。三张卡串成"仿真 → 翻译 → 验证"闭环。

资源池（4 张）：计算资源调度 k3s 容器，服务注册从 188 个节点自动生成实例，分布式缓存由 Autopilot 自动调度，数据资源追踪三类数据。

策略管理（2 张）：Kubernetes CRD 是唯一策略入口，OVS 流表是硬件落地点。

端到端链路：CRD 提交 → 仿真推演 → 规则翻译 → ONOS 下发 → OVS 执行。

---

## 第 3 页 · 动态拓扑仿真控制

仪表盘核心。六个指标：仿真状态、推进进度、活跃节点(54)、活跃链路(638–648)、翻译成功率(2%→83.3%)、仿真时间。下方依次为进度条、控制按钮（启动/暂停/停止，极速/1×/10×）、折线图（链路数与节点数每秒更新，每 60 秒跳变）、步进日志。

后台每 100 毫秒推进一步，6000 步覆盖 10 分钟仿真，每步重算卫星可见性和链路通达性。演示：启动 → 观察折线图 → 切 10 倍速 → 完成后成功率跳绿。

---

## 第 4 页 · 规则翻译

将 S3 路由规则翻译为交换机流表。输入 36,000 条规则，输出 30,000 条流表（去重合并），成功率 83.3%。点击执行按钮后，加载规则、逐条翻译、推送 OVS，几秒完成。83.3% 是因为同时间步不同源到同目标的路径被自动去重。柱状图记录每次翻译成功率的历史趋势。

---

## 第 5 页 · 网络拓扑

ONOS 实时发现的物理网络：7 台交换机（1 在线）、13 台主机。每台设备标注 IP 和接入端口。数据通过 ARP/LLDP 自动发现，15 秒刷新——设备上线即出现，离线即消失，不是静态配置。

---

## 第 6 页 · 计算资源 & 服务注册

计算资源：管理 k3s 集群容器调度，显示就绪节点和运行容器。容器类型包括照片处理、控制中继、传感器融合，标注 CPU 和内存。点击按钮即可部署新容器。

服务注册：从 S3 的 188 个节点自动生成服务实例。地面站注册通信、计算、数据三类；卫星注册通信；无人机注册感知。每次加载 S3 自动重建，拓扑变化时服务自动增减，无需手工配置。

---

## 第 7 页 · 分布式缓存 & 数据资源

分布式缓存：7 节点存储池，总容量 2176 MB。进度条绿/黄/红三色区分健康状态。Autopilot 每 50 步自动扫描：单节点超 90% 驱逐旧数据，多节点超 75% 跨节点迁移。系统自主决策执行，无需人工干预。

数据资源：管理感知、任务、态势三类数据。每条数据标注优先级、类型、大小、副本数和存放位置。紧急数据双副本防丢失，大文件独立副本策略。

---

## 第 8 页 · 策略管理

左侧 CRD 是唯一策略入口：先安装资源定义，再提交策略指定源/目标/优先级。后台每 10 秒检测变化，自动触发规则重翻译。所有策略必须通过 CRD，不允许绕过。

右侧 OVS 流表是闭环落地点：流表分三层，自定义流表每条来自 S3 翻译，标注优先级、匹配条件和转发指令。前端、ONOS、交换机三条路径可交叉验证，结果一致。

---

## 第 9 页 · 总结

刷新：仿真控制每秒轮询，其余 8 张卡片 15 秒全局刷新，按钮即时刷新。

三点：数据全自动发现非静态配置；控制闭环从策略到硬件完整可验证；Autopilot 自主决策无需人工干预。
