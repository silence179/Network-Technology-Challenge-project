# ERRORS RECORD
**5th Feb** 
获得s1, s2的csv，验证完成后便与s5（前端）对接

1. s2的csv并未按60秒切分好
2. 在前端实现后，发现地面站点和无人机的z轴都高于现实约几百米

**8th Mar**
要將s1-s4的代碼在同一台機器上運行

1. s2沒將依賴倉庫SAREnv傳上共享倉庫且未提交說明文檔導致無法正常運行s2的代碼
2. s2的`generate_dataset.py`腳本比原依賴倉庫多了一個屬性︰"sizes"導致無法運行
3. s3產生的`topology_links`和`routing_rules`直接生成於s3的文件夾，而S4則是從S3的`output`中的`links`,`rules`找對應的.csv和.json文件。建議S3修改輸出文件部分的代碼，更好使用及管理輸出文件。
4. s1,s2的腳本模擬的總時長不一致（10min&7min）
5. 優化s1,s2的output使s3順利讀取

**26th Mar**尸
1. s4中的`config.py`︰
   新版日志解决了以下缺陷
✅ 单例 Logger，永不重复创建
导入 100 次也只会创建一次，不会重复打印
✅ 自动去重 Handler
不会出现日志越打越多、重复 N 行的问题
✅ 级别严格分离
info.log 只存 info
warning.log 只存 warn+
error.log 只存 error+
debug.log 存全部
✅ 自带日志切割
每个日志最大 10MB，最多保留 5 份，自动覆盖
永远不会占满硬盘
✅ 控制台彩色输出保留
和你原来效果一样，但更稳定
✅ 无资源泄漏
Python 会自动管理文件句柄，安全可靠
2. s4中的`generate.py`
重构核心优化点
```
1. 解决硬编码问题（最高优先级）
所有魔法值（时间、周期、内容 ID 等）抽离为可配置常量，集中放在文件顶部，修改时无需改动业务逻辑；
新增 enable_extended_stages 参数，通过开关控制扩展阶段（低清图像 / 4K 视频 / 汇聚指令），无需注释 / 删除代码；
2. 性能优化
优化循环步长：原版本按 100ms 步长迭代（90% 空跑），重构后按「最小周期」迭代（默认 1000ms），减少无效循环；
避免重复计算：扩展阶段的周期判断仅在启用时执行，减少不必要的条件判断；
3. 保证仿真可复现
新增 random_seed 参数（默认 42），通过固定随机种子，确保每次运行生成的请求时间完全一致；
4. 鲁棒性提升
完善参数校验：对 uav_list/main_gs/max_time_ms 做合法性检查，抛出明确的 ValueError，避免非法输入导致崩溃；
类型注解：为函数入参、返回值添加完整的类型提示，提升 IDE 友好性和代码可读性；
5. 可维护性提升
函数文档字符串：详细说明参数、返回值、异常，新人可直接通过文档理解用法；
结构化逻辑：按「参数校验→随机种子→请求生成→排序返回」拆分逻辑，层次清晰；
测试示例：新增 __main__ 测试块，可直接运行验证功能，方便调试；
6. 扩展性预留
常量均采用「大写 + 下划线」命名规范，符合 Python 配置项约定；
扩展阶段的配置独立拆分，新增业务场景时仅需添加常量和对应的逻辑块；
```